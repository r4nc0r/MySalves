<Window x:Name="mywindow"
        x:Class="Wrappanel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wrappanel"
        mc:Ignorable="d"
        Title="MainWindow" Height="250" Width="525">
    
    <!--
        <WrapPanel Width="400" Height="50">
            <TextBox Name="txt1" Text="" Width="100" Height="100"></TextBox>
        <TextBox Name="txt2" Text="{Binding ElementName=txt1, UpdateSourceTrigger=LostFocus, Path=Text, Mode=OneWay}" Width="100"></TextBox>
        <TextBox Name="txt3" Text="{Binding ElementName=txt2, UpdateSourceTrigger=LostFocus, Path=Text, Mode=OneWay}" Width="100"></TextBox>
        <TextBox Name="txt4" Text="{Binding ElementName=txt3, UpdateSourceTrigger=LostFocus, Path=Text, Mode=OneWay}" Width="100"></TextBox>
        </WrapPanel>
    -->
</Window>
